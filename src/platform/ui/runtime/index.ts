/**
 * UI Runtime — public component surface (@/platform/ui).
 *
 * primitives/  — internal styled HTML (do not import in app code)
 * components/  — Ui* identified components + factories
 */

export * from './public-api';
